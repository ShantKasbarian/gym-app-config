MAIN-APP:
  URL: "http://localhost:8080"
  GET-ALL-WORKLOADS: "/trainings"

WORKLOAD-APP:
  URL: "http://localhost:8000"

MODIFY-WORKLOAD:
  URL: "http://workload-app/workloads"
  QUEUE-NAME: "modify-workload-queue"

API-KEY:
  HEADER-NAME: "X-API-KEY"

WORKLOAD:
  REQUEST:
    QUEUE-NAME: "workload-request-queue"

  RESPONSE:
    QUEUE-NAME: "workload-response-queue"

